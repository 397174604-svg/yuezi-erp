# 财务管理字段级复刻交接

## 当前完成范围

财务管理已从通用占位页切换为独立工作台，覆盖原 ERP 的 11 个菜单：

1. 新增收款
2. 收款管理
3. 退款申请
4. 退款审核
5. 欠款审核
6. 换货审核
7. 发票管理
8. 部门物料预算
9. 我的费用
10. 费用审核
11. 付款管理

页面级筛选字段、表格列、状态、按钮、审核/打款弹窗和主要下拉枚举均按原 ERP 分页采集结果配置。2026-07-27 已切换到本机 MySQL `yuezi` 和真实后端接口：列表无数据时返回空列表，不再生成脱敏演示记录；未配置支付通道时星支付、扫码支付会明确阻止执行，不伪造支付成功。

## 核心实现文件

- `src/config/finance-pages.js`：11 个页面的字段、列、枚举、操作和弹窗配置。
- `src/views/erp/finance-workbench/index.vue`：财务专用表单、列表、详情、审核和打款工作台。
- `src/api/erp-finance.js`：财务页面接口封装。
- `server/mvp_api.py`：财务查询、选择器、保存、审核、核销、打款及按钮级权限接口。
- `database/mysql/migrations/V20260727_013__finance_operations.sql`：财务扩展、核销、退款扩展、预算、欠款审核、换货审核、费用扩展和付款表。
- `src/config/erp-menu.js`：财务菜单统一映射到专用工作台。
- `src/router/index.js`：注册财务工作台路由组件。
- `scripts/serve-dist.cjs`：在开发服务器无法启动时预览生产构建。

## 已核对的关键业务枚举

- 新增收款：收款类型、款项类别、结算方式、收款分店、收入类型、收款银行、是否开票。
- 收款管理：12 个款项类别、18 个支付方式、审核状态和结算核销状态。
- 退款：8 个退款类型、2 个退款渠道、5 个退款状态及退款支付方式。
- 欠款审核：待审核、已通过；审核结果为审核通过、审核不通过。
- 换货审核：已通过、待审核、已驳回。
- 部门物料预算：待提交、审核中、审核通过、已下达、驳回。
- 我的费用：待提交、已提交、已审批、已打款、驳回。
- 费用审核：已提交、已审批、已打款、驳回。
- 付款管理：审核中、审核通过、审核不通过；待打款、已打款。

## 验收记录

- 目标文件 ESLint：通过。
- `vue-cli-service build`：通过，仅保留项目原有体积告警。
- 浏览器逐页回归：11 个菜单标题、筛选项和表格列均能随路由正确更新。
- 退款申请“打款”弹窗：退款金额、实退金额、退款方式、打款时间、收款人、银行、银行卡号、打款说明、微信、短信均已显示。
- 新增收款“收款人”：使用业务员账号弹窗选择器，支持按账号、姓名、部门和门店查询并回填账号。
- 新增收款“选择客户”：使用现有客户弹窗选择器，支持按客户账号、姓名、手机号和业务员查询并关联客户编号。
- 新增收款“上传附件”：使用文件选择上传控件，显示待保存附件清单，不再使用文本输入框。
- 收款管理新增“客户状态”16 项多选和“门店”3 项多选，选项顺序与原 ERP 一致。
- 收款管理“不显示”区独立为一排：核销、会员卡、优惠券、收款为0、admin单据、已结账出院；默认勾选状态按原 ERP 保留。
- 收款管理底部拆分为“收款列表 / 预收款列表 / 欠款列表 / 预授权列表”四个页签，分别使用 26 列、20 列加功能列、15 列加序号列、15 列加勾选列与功能列的独立表头。
- 预授权列表按原系统保留：单据编号、客户姓名、手机号、部门、收款人、单据日期、款项类别、支付方式、单据金额、制单人、财务审核、审核人、审核意见、收款分店、销售单号、功能。
- 我的费用状态下拉：5 项完整。
- 付款管理审核/打款状态下拉：完整。
- 许曼账号只返回黄河路店；管理员返回建设路店和黄河路店。
- 许曼账号退款“流程审批”按旧系统按钮授权返回 403，不能越权审批。
- 财务 11 个读取接口、员工/客户选择器与门店选项接口均返回 200。
- 联调过程未写入测试业务记录，财务业务表记录数仍为 0。
- 本地财务页面控制台错误：0。

## 财务列表页顶部工具栏

2026-07-23 按原 ERP 实际菜单链接逐页重新采集，按钮名称与顺序如下：

| 页面 | 原系统地址 | 顶部功能按钮 |
| --- | --- | --- |
| 收款管理 | `Page/ReceivePayManager/ReceiveBill.aspx?navid=90` | 添加、星支付、开具发票、编辑、删除、导出、打印、审核、批量审核、核销、反审核、手续费、扫码支付 |
| 退款申请 | `Page/ReceivePayManager/ReFoundBill.aspx?navid=95` | 添加、编辑、删除、打印、提交、打款、导出 |
| 退款审核 | `Page/ReceivePayManager/ReFoundshenhe.aspx?navid=96` | 流程审批、反审核、撤回 |
| 欠款审核 | `Page/ContractManager/DFCFSH.aspx?navid=281` | 审核 |
| 换货审核 | `Page/ReceivePayManager/BarterOrder.aspx?navid=653` | 审核、删除 |
| 发票管理 | `Page/ReceivePayManager/InvoiceList.aspx?navid=572` | 导出、删除 |
| 部门物料预算 | `Page/ServiceCenter/Budgetplan.aspx?navid=251` | 添加、流程审批、编辑、删除、提交、生成采购计划、导出 |
| 我的费用 | `Page/SaleFee/CheckedSelect.aspx?navid=317` | 添加、编辑、删除、导出、打印、打款、反审核、提交 |
| 费用审核 | `Page/SaleFee/CheckedSelectSH.aspx?navid=607` | 流程审批 |
| 付款管理 | `Page/ReceivePayManager/PayBillshenhe.aspx?navid=415` | 导出 |

本地页面把该工具栏放在查询条件上方。删除、审核、撤回、核销、手续费、开票和内部打款等动作均调用本地 MySQL 后端，并按旧系统菜单按钮权限校验。星支付、扫码支付属于外部资金动作，当前未配置支付通道，因此只提示不可执行。

## 本地运行

后端：

```powershell
$env:ERP_DB_PASSWORD='你的本机 MySQL 密码'
npm run api:mvp
```

前端另开终端：

```powershell
npm run dev:mvp
```

当前验证地址为 `http://127.0.0.1:9530/#/finance/item-2`，后端端口为 `3000`。

## 后续继续复刻时的规则

个人技能 `$replicate-legacy-erp` 已创建在：

`C:\Users\39717\.codex\skills\replicate-legacy-erp\SKILL.md`

继续爬取其他模块时必须按页面分别采集字段、默认值、提示文字、下拉项、联动关系、表格列、按钮和弹窗；不能因字段同名就复用全局枚举。所有依赖型下拉都要逐个测试父选项，并验证切换父选项后旧值会被清空。
