# 客房管理字段级复刻交接记录

更新日期：2026-07-24

## 2026-07-24 智能排房二次字段级复刻

- 原页证据：`Page/GuestRoomManger/BrainpowerHouseList.aspx?navid=517`；本地路由为 `/room/item-4`。
- 已将原来的通用筛选表格改为智能排房专用页面，查询区按原页顺序复刻：
  - 分店：中心广场旗舰店、黄河路轻奢店；
  - 入住日期：开始日期、天数、“天”单位、结束日期；
  - 房间类型：保留“请选择”，并录入原页全部 18 个房间/套餐类型；
  - 楼层：只允许输入数字；
  - 查询。
- 已复刻两组推荐结果：
  - “推荐一（注：客户不需换房,系统至多推荐20个房间选择）”，每张卡片显示入住区间、房号、房型和天数；
  - “推荐二（注：2房间拼房，客户需换1次房）”，每个方案由两张房间片段和换房指示组成；
  - 点击方案使用原页粉色边框表示选中；未选择方案点击“订房”时提示“请选择想订的房间!”。
- 已复刻“订房”弹窗字段与联动：
  - 房间号1、预住日期1、房间号2、预住日期2为只读；
  - 客户姓名通过弹窗选择，不使用自由输入；
  - 预住天数、分娩日期、分娩方式、陪护人电话、分娩医院、备注；
  - 分娩医院选择“-其他-”时显示“其他分娩医院”输入框；
  - 客户为空和预住天数为空时分别显示原页校验提示。
- 已复刻客户选择弹窗的查询项、未订房/已订房状态和主要业务列，并支持单击选中、双击带回；客户及房间均为脱敏演示数据。
- 当前完成级别为“字段、枚举、推荐卡片及弹窗交互级复刻”。推荐算法、客户合同校验、真实可售房库存及保存动作尚未接入真实后端。
- 新增专用组件：
  - `src/views/erp/room-workbench/SmartRoomAllocation.vue`
  - `src/views/erp/room-workbench/RoomRecommendationSegment.vue`
- 本轮定向 ESLint 与 `npm run build:prod` 均通过；生产构建仅有项目既有的资源体积警告。

## 2026-07-24 房型趋势二次字段级复刻

- 原页证据：`Page/GuestRoomManger/RoomTreadNew.aspx?navid=587`，默认图形 iframe 为 `RoomTreadGraph.aspx`。
- 查询区已按原页恢复为：门店、预定时间、28 天、结束日期、房型占用数和查询。
- “房型占用数:”是分组标签，不是第五个复选框；同一横排的 4 个复选框依次为“入住客户数、订房客户数、维修（占用）房数、合同预住数”，默认全部勾选。
- 红色业务说明已按原文补齐：“房型占用数是由：包括了入住客户数，订房客户数，维修(占用)房数和合同预住数组成”。
- 原矩阵视图已替换为 ECharts 折线图，复刻以下图表契约：
  - 标题“房型可用趋势图”，副标题“每日房型剩余数”；
  - 横轴为 `MM-DD` 日期分类轴且不留边界间隔，纵轴为数值轴；
  - 每个房型和“总剩余数”均为折线系列，所有数据点显示数值；
  - “总剩余数”使用 5px 加粗折线；
  - 轴触发提示框显示系列值及“余房占比 xx.xx%”；
  - 工具箱提供保存为图片。
- 图例按原页的门店依赖切换：
  - 中心广场旗舰店：豪华套房、舒适大床、温馨雅间、尊享套房、舒适小套、5楼VIP、3楼VIP、总剩余数；
  - 黄河路轻奢店：豪华套房、舒适大床、至尊女王、总统套房、总剩余数。
- 查询按钮使用筛选快照重绘图表；改变门店或指标后点击查询才更新，保持原页交互方式。
- 当前折线数值为脱敏演示聚合数据，属于“字段及交互级复刻”，尚未接入原 ERP 或本项目真实客房库存后端。
- 本轮定向 ESLint 与 `npm run build:prod` 均通过；本地浏览器确认 4 项复选框纵坐标一致、两家门店图例正确切换、默认项全部勾选，控制台错误为 0。

## 本轮范围

已按原“妈妈宝盒 ERP”客房管理菜单完成 15 个子页面的首轮字段级复刻：

| 序号 | 页面 | 原系统路径 |
| --- | --- | --- |
| 1 | 房态图 | `Page/SalerManager/RoomMangerNew.aspx?navid=418` |
| 2 | 房态趋势 | `Page/GuestRoomManger/RoomTread.html?navid=526` |
| 3 | 房型趋势 | `Page/GuestRoomManger/RoomTreadNew.aspx?navid=587` |
| 4 | 智能排房 | `Page/GuestRoomManger/BrainpowerHouseList.aspx?navid=517` |
| 5 | 可售房统计 | `Page/GuestRoomManger/BrainpowerReport.aspx?navid=567` |
| 6 | 房型列表 | `Page/GuestRoomManger/HouseList.aspx?navid=424` |
| 7 | 订房管理 | `Page/SalerManager/RoomTD.aspx?navid=375` |
| 8 | 入住管理 | `Page/SalerManager/RoomXZ.aspx?navid=558` |
| 9 | 续住信息 | `Page/GuestRoomManger/ContinueInfo.aspx?navid=615` |
| 10 | 换房申请 | `Page/SalerManager/ChangeRoomApply.aspx?navid=584` |
| 11 | 物品赠送 | `Page/GuestRoomManger/ReturnOfGoods.aspx?navid=284` |
| 12 | 客房服务 | `Page/GuestRoomManger/CleanApply.aspx?navid=236` |
| 13 | 外出申请 | `Page/NursingManager/OutRecord.aspx?navid=113` |
| 14 | 物品借还 | `Page/GuestRoomManger/GiftsManger.aspx?navid=235` |
| 15 | 洗衣管理 | `Page/GuestRoomManger/Laundry.aspx?navid=238` |

## 已实现内容

- 为客房管理建立独立工作台，不再使用通用 ERP 表格页。
- 复刻各页面标题、筛选字段、下拉选项、默认值、业务工具栏、表格列及主要状态。
- 房态图按楼层/房型分组展示，并录入原系统两家门店、18 个房型、朝向与展示类型选项。
- 房态图已补齐原页面 14 个顶部业务动作：商品销售、订房、入住、续住、换房、退房、结账、入住通知单、客房服务申请、服务预约、房型订房、维修/脏房、跨店订房、跨店换房。
- 房间卡片展示房号、详情数量、脱敏住客、已住/合同天数和入住时间区间；点击卡片后可作为工具栏操作的当前房间。
- 点击房间卡片内任意住户可打开“客户明细”，包含原入口的 17 个业务页签；“客户详细信息”已按原页面复刻客户资料、账户与收款、客户状态及意向信息字段。
- 点击房号右侧“详情(n)”可打开“订房详情”，顶部复刻当前入住客户、预入住/入住/退房时间、预住/已住/剩余天数、陪护、押金和房间属性；下方复刻“客户的房间记录”“过去入住信息”“未来预定信息”三个横向列表及各自表头。
- 房态趋势实现按日期展开的时间轴表格。
- 房型趋势实现入住、订房、维修占用、合同预住统计矩阵。
- 智能排房实现两类推荐卡片、选中状态、订房表单和客户弹窗选择；当前仍由脱敏 Mock 推荐结果驱动。
- 订房、入住、续住、换房、赠送、客房服务、外出、借还、洗衣等页面已配置对应业务按钮和本地演示交互。
- 导出按钮生成 CSV，打印按钮调用浏览器打印；查询、重置、分页和状态动作接入本地 Mock 数据。
- 演示数据全部为脱敏样例，不包含原系统真实业务数据。

## 主要实现文件

- `src/config/room-pages.js`：15 个页面的字段、选项、列、按钮和特殊视图配置。
- `src/views/erp/room-workbench/index.vue`：客房管理独立工作台及业务交互。
- `src/api/erp-room.js`：客房管理请求接口。
- `mock/erp-room.js`：脱敏演示数据和本地动作接口。
- `src/config/erp-menu.js`：客房菜单统一映射到独立工作台。
- `src/router/index.js`：客房工作台路由注册。
- `mock/index.js`：客房 Mock 模块注册。

## 验证记录

- 字段级复刻 skill 已通过 `quick_validate.py` 校验。
- 客房相关源文件通过 ESLint。
- `npm run build:prod` 构建成功；仅保留项目原有的资源体积提示。
- 本地 `/room/item-1` 至 `/room/item-15` 已逐页重新加载检查。
- 已核对主要工具栏、筛选项、表头、房态卡片和趋势表格。
- 浏览器控制台未发现页面运行错误。

## 尚需二次深挖

以下内容不能标记为最终字段级完成：

1. 原系统“外出申请”“物品借还”“洗衣管理”的新增窗口使用 iframe，自动读取多次超时。本地表单已依据列表字段和可观察业务流程实现，但仍需逐字段人工复核原弹窗的必填规则、控件类型、默认值和联动。
2. 续住、换房、审核、退订、服务确认等二级弹窗已具备演示交互，仍需继续核对原系统中的提示文字、校验规则、审批意见和状态迁移。
3. 当前数据与动作由 Mock 层提供，不代表真实后端、数据库、权限、金额计算和审批流已经接通。

## 下一步顺序

1. 深挖并补齐上述三个 iframe 新增表单。
2. 按工具栏逐按钮核对弹窗、确认提示、状态变化和权限条件。
3. 建立客房、合同、客户、收款之间的真实关联接口。
4. 对订房、入住、续住、换房、退房执行跨模块回归测试。
