# 仓存管理迁移交接

## 2026-07-25 顶部工具栏图标补齐

- 仓存管理 24 个子页面统一开启顶部动作图标，并按动作语义覆盖删除、计划下达、导出、导入、审核、反审核、盘点、入库、核销、生成单据等操作。
- 只读抽查原采购计划页 `Page/ICManager/PurchasingplanListNew.aspx?navid=337`，原工具栏为“删除、计划下达、导出”且三项均有独立图标。
- 本地 `/warehouse/item-1` 已验证三项图标节点和动作顺序一致；生产构建与 ESLint 通过。

## 2026-07-24 字段级审计更新

- 24/24 页已只读核验并接入原版顶部工具栏与主查询区：92 个工具栏动作、157 个原始查询控件、45 个查询区动作。
- 采购订单完成状态单选组、采购入库/退货复选组及仓库下拉全集已按原页面复刻。
- 已修复带“统计/报表/汇总”标题的仓存页面被通用报表组件截获的问题。
- 已执行 24 条路由的 DOM 对照，全部通过且浏览器错误为 0。
- 下方旧版“待核验”内容是骨架阶段历史；库存状态机、金额、审批和真实写入仍待继续。

更新时间：2026-07-23

## 证据边界

- 已确认：本地 `src/config/erp-menu.js` 中“仓存管理”的 24 个子菜单名称与顺序。
- 未确认：原 ERP 页面 URL/navid、页面类型、工具栏按钮及顺序、查询字段、下拉选项、默认值、表格列、汇总公式、新增/编辑/审核弹窗、依赖联动、打印与导入格式。
- 原因：本并行 Agent 未继承已登录浏览器会话，无法安全读取原系统页面。
- 因此当前实现统一标记为 `待原系统二次核验 / Visible`，不得称为字段级完成。
- 页面数据均为脱敏演示数据；保存、审核、出入库、调拨、盘点、付款和核销均为 Mock 演示，不产生真实业务状态。

## 菜单清单

1. 采购计划
2. 采购订单
3. 采购单审核
4. 其他入库
5. 采购入库
6. 领料申请
7. 销售出库
8. 领料申请(去金额)
9. 调拨管理
10. 退货管理
11. 盘点管理(NEW)
12. 报损管理
13. 期初数据导入
14. 物料库存预警
15. 期初数据查询
16. 赠送清单计划
17. 收发存汇总统计
18. 库存明细统计
19. 部门领料统计
20. 仓库库存查询
21. 采购明细报表
22. 预付款列表
23. 付款单列表
24. 应付账款明细表

## 独立实现文件

- `src/config/inventory-pages.js`：24 个页面的预置配置，所有字段均带未验证语义。
- `src/views/erp/inventory-workbench/index.vue`：独立仓存工作台，覆盖交易、审批、导入、预警、统计和应付页面形态。
- `src/api/erp-inventory.js`：仓存模块接口边界。
- `mock/erp-inventory.js`：空列表和演示操作响应，不包含真实业务数据。

## 根任务集成

本并行 Agent 未修改共享文件。根任务需完成：

1. 在 `src/config/erp-menu.js` 中将 `warehouse` 映射从 `inventory` 调整为 `inventory-workbench`。
2. 在 `src/router/index.js` 增加：
   - `const inventoryWorkbenchPage = () => import('@/views/erp/inventory-workbench/index')`
   - `if (pageType === 'inventory-workbench') return inventoryWorkbenchPage`
3. 在 `mock/index.js` 增加：
   - `const erpInventory = require('./erp-inventory')`
   - 在 `mocks` 数组展开 `...erpInventory`

## 二次核验顺序

对每个原 ERP 页面独立建立证据行，依次核验：

1. URL/navid 与页面类型。
2. 顶部业务按钮的标签、顺序、位置、选中规则及真实行为。
3. 查询字段、控件类型、选项顺序、默认值、必填/禁用状态与联动。
4. 表格列、隐藏技术列、金额/数量格式及底部汇总公式。
5. 新增、编辑、审核、反审核、出入库、调拨、盘点、报损、付款、核销弹窗。
6. 期初导入模板、文件类型/大小、校验结果与错误反馈。
7. 采购—入库—应付—付款—核销，以及领料/销售—出库—库存扣减状态链。

完成原页证据后，应逐项替换预置草案，并将完成度分别更新为 Schema-faithful、Interaction-faithful 或 Integrated。
