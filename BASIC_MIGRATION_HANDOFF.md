# 基础资料模块复刻交接

## 2026-07-25 清除审计草案页面并补齐图标

- `src/views/erp/basic-workbench/index.vue` 已移除页面内全部“列表字段结构草案、脱敏演示、候选表单、本页审计缺口、BED-DEMO、待核验”开发占位内容。
- 当前基础资料业务页只渲染已核验的原版顶部工具栏、主查询区和原表头空表格；不再展示推断字段或演示记录。
- 基础资料 19 个子页面统一开启顶部动作图标。
- 只读复核床位管理 `Page/BasicInfo/BunkList.aspx?navid=624`：工具栏为“添加、编辑、删除”；查询区为“床位名称、床位类型、搜  索”；床位类型为“--请选择--、高级床位、豪华床位”；表头为“序号、床位编号、床位名称、床位类型、门店、创建人、创建时间”。
- 本地 `/basic/item-19` 已验证禁用词清零、工具栏图标完整、查询条件与表头落位；生产构建与 ESLint 通过。

## 2026-07-24 字段级审计更新

- 19/19 页已只读核验并接入原版顶部工具栏与主查询区：75 个工具栏动作、68 个查询控件、22 个查询区动作。
- 19 个原页面 URL 与 `navid` 已补入 `src/config/original-page-evidence.js`。
- 已修复“报表模板”等标题被通用报表组件截获的问题。
- 已执行 19 条路由的 DOM 对照，全部通过且浏览器错误为 0。
- 下方旧版“待核验”内容是骨架阶段历史；完整表格、表单、弹窗和真实主数据写入仍待继续。

## 结论

- 当前仓库 `src/config/erp-menu.js` 的“基础资料”分组包含 **19 个**子菜单。
- 本次新增的独立配置 `src/config/basic-pages.js` 同样包含 **19 个**页面定义，标题和顺序逐项对应。
- 子任务浏览器环境无法连接已登录的原 ERP；根任务随后在主登录会话中只读核验了 19 个菜单的 **原页面 URL 与 navid**，并已回填配置。页面模式、字段、选项、默认值、层级、联动、按钮、表头、表单及业务行为仍未逐页核验。
- 当前成果完成等级仅为 **Visible**：可运行的页面承接骨架与脱敏 Mock 演示。不能宣称 Schema-faithful、Interaction-faithful 或 Integrated。
- 页面内所有推断的查询字段、列表列、表单字段和结构类型均显著标注“待原系统二次核验”；没有实现真实保存、删除、审核、导入、导出、打印或持久化。

## 新增文件

- `src/config/basic-pages.js`
  - 19 个菜单的独立配置草案。
  - 每个字段和列都包含 `verified: false`。
  - 每页已回填主会话核验过的 `originalUrl`、`originalNavid`，内部字段仍为 `verified: false`。
- `src/views/erp/basic-workbench/index.vue`
  - 基础资料独立工作台。
  - 显示菜单覆盖数、筛选/列表/表单草案数量和逐项审计缺口。
  - 页面不会把草案按钮伪装成原系统工具栏。
- `src/api/erp-basic.js`
  - 查询 Mock 数据及本地预览接口封装。
- `mock/erp-basic.js`
  - 返回空列表，由页面生成脱敏演示记录。
  - 预览接口明确返回 `persisted: false`。

按任务约束，未修改：

- `src/router/index.js`
- `src/config/erp-menu.js`
- `mock/index.js`

主任务集成时需要由根任务统一把 `basic` 分组映射到 `basic-workbench`，注册组件和 Mock。

## 菜单与证据矩阵

| 序号 | 菜单标题 | 本地结构草案 | 原 URL / navid | 字段、控件、默认值 | 工具栏、列、表单、联动 | 当前等级 |
|---:|---|---|---|---|---|---|
| 1 | 职员档案 | 列表 | 已核验（见配置） | 待原系统二次核验 | 待原系统二次核验 | Visible |
| 2 | 基础项目 | 树 + 列表 | 已核验（见配置） | 待原系统二次核验 | 待原系统二次核验 | Visible |
| 3 | 物料档案 | 树 + 列表 | 已核验（见配置） | 待原系统二次核验 | 待原系统二次核验 | Visible |
| 4 | 客房档案 | 树 + 列表 | 已核验（见配置） | 待原系统二次核验 | 待原系统二次核验 | Visible |
| 5 | 满意度调查表模板 | 模板 | 已核验（见配置） | 待原系统二次核验 | 待原系统二次核验 | Visible |
| 6 | 调查表管理 | 模板 | 已核验（见配置） | 待原系统二次核验 | 待原系统二次核验 | Visible |
| 7 | 仓库档案 | 树 + 列表 | 已核验（见配置） | 待原系统二次核验 | 待原系统二次核验 | Visible |
| 8 | 供应商档案 | 列表 | 已核验（见配置） | 待原系统二次核验 | 待原系统二次核验 | Visible |
| 9 | 资金账户 | 列表 | 已核验（见配置） | 待原系统二次核验 | 待原系统二次核验 | Visible |
| 10 | 报表模板 | 模板 | 已核验（见配置） | 待原系统二次核验 | 待原系统二次核验 | Visible |
| 11 | 护理模板 | 模板 | 已核验（见配置） | 待原系统二次核验 | 待原系统二次核验 | Visible |
| 12 | 任务管理 | 树 + 列表 | 已核验（见配置） | 待原系统二次核验 | 待原系统二次核验 | Visible |
| 13 | 服务时间设置 | 设置 | 已核验（见配置） | 待原系统二次核验 | 待原系统二次核验 | Visible |
| 14 | 项目手工费设置 | 设置 | 已核验（见配置） | 待原系统二次核验 | 待原系统二次核验 | Visible |
| 15 | 提成比例设置 | 设置 | 已核验（见配置） | 待原系统二次核验 | 待原系统二次核验 | Visible |
| 16 | 设备管理 | 列表 | 已核验（见配置） | 待原系统二次核验 | 待原系统二次核验 | Visible |
| 17 | 业绩目标设置 | 设置 | 已核验（见配置） | 待原系统二次核验 | 待原系统二次核验 | Visible |
| 18 | 优惠金额授权 | 设置 | 已核验（见配置） | 待原系统二次核验 | 待原系统二次核验 | Visible |
| 19 | 床位管理 | 树 + 列表 | 已核验（见配置） | 待原系统二次核验 | 待原系统二次核验 | Visible |

上表“本地结构草案”仅是为了让后续审计结果有落点，不是原 ERP 页面类型证据。

## 原系统二次核验顺序

逐页执行只读审计，每页单独记录，禁止跨页复用枚举：

1. 从原 ERP 左侧菜单提取准确标题、URL 与 `navid`。
2. 记录页面模式、区域顺序、默认查询条件和默认日期范围。
3. 记录每个查询控件的标签、类型、占位语义、完整选项顺序、默认值、必填/禁用/只读状态。
4. 对所有树选择器记录父子层级、默认展开、可选节点和多选规则。
5. 对所有依赖控件逐个切换控制项，验证从属选项、启用状态及旧值清空行为。
6. 分开记录查询按钮与业务工具栏，核对标签、顺序、位置、选中行要求和权限隐藏。
7. 记录完整表头、隐藏技术列、排序、格式、汇总行及公式。
8. 打开但不提交新增/编辑/配置对话框，记录分组、字段、必填、校验、提示、上传、明细表和底部动作。
9. 本地实现后逐页全量刷新回归，确认不是哈希路由残留组件，并检查控制台错误。

## 重点依赖场景（全部待核验）

- 基础项目：项目分类 → 项目；业务类型 → 可用单位/计价规则。
- 物料档案：物料分类 → 物料；计量单位、库存单位与换算关系。
- 客房档案：门店 → 楼层 → 房间；房型 → 房间属性。
- 满意度调查表模板 / 调查表管理：题型 → 选项编辑器；评分规则 → 结果区间。
- 服务时间设置：门店/服务类型 → 项目 → 可预约时段。
- 项目手工费设置：项目类型 → 项目；计费规则 → 金额或比例控件。
- 提成比例设置：业务类型/人员范围 → 计算基数与比例阶梯。
- 业绩目标设置：周期 → 门店/部门/人员 → 可配置目标指标。
- 优惠金额授权：授权对象类型 → 角色/人员；业务类型 → 单笔与周期额度。
- 床位管理：门店 → 楼层 → 房间 → 床位。

这些关系只是待验证清单，不能作为已实现业务规则使用。

## 集成建议

根任务统一集成以下三处，避免多 Agent 同时修改共享文件：

1. `src/config/erp-menu.js`
   - 在通用 `basic/settings` 分支之前增加：`if (groupKey === 'basic') return 'basic-workbench'`。
2. `src/router/index.js`
   - 懒加载 `@/views/erp/basic-workbench/index`。
   - 在 `getPageComponent` 中映射 `basic-workbench`。
3. `mock/index.js`
   - 引入并展开 `./erp-basic`。

## 验证记录

- 菜单标题差异检查：通过；仓库菜单 19、独立配置 19、缺失 0、额外 0。
- Targeted ESLint：通过；校验 `src/config/basic-pages.js`、`src/views/erp/basic-workbench/index.vue`、`src/api/erp-basic.js`。
- `git diff --check`：通过。
- 生产构建与浏览器回归：留给根任务在共享路由和 Mock 集成后统一执行。
