# 风控服务迁移交接

## 2026-07-24 字段级审计更新

- 原“悦禧风控”页已确认没有顶部工具栏和主查询区，本地不再展示虚构的风险事件/处置工作台。
- 页面已改为原版三栏并排会员服务矩阵，共 83 条固定服务，列为“序号 / 服务项目 / 白银会员 / 黄金会员”。
- 浏览器对照已通过：表格 3 个、服务 83 行、运行时错误 0。
- 当前入口为 `src/views/erp/risk-workbench/RiskServiceMatrix.vue`；下方旧版骨架说明不再代表当前实现。

更新时间：2026-07-23

## 本轮结论

本轮按照 `replicate-legacy-erp` Skill 交付风控服务的独立配置、工作台、API 和 Mock 骨架。

当前 Agent 无法访问主任务中的已登录浏览器会话，可用浏览器列表为空。因此，本轮没有读取原 ERP 的“悦禧风控”页面，也没有把任何筛选、按钮、列、枚举、弹窗或流程标记为已验证。

已确认的唯一页面事实：

- 一级菜单：`风控服务`
- 子菜单：`悦禧风控`
- 子菜单顺序：第 1 项，且本地菜单证据中只有这一项

本地 `ERP_MIGRATION.md` 只提供领域级描述：“异常规则、风险事件和处置”。它不是原页面字段证据。基于这一描述搭建的风险事件、处置跟进和异常规则三个页内草案，均在配置与 UI 中显著标记为“待原系统二次核验”。

## 页面证据矩阵

| 页面 | 菜单标题 | 原 URL / navid | 页面形态 | 查询字段/默认值 | 工具栏 | 表格 | 弹窗/流程 | 完成等级 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 风控服务唯一子菜单 | 悦禧风控（已确认） | 待核验 | 待核验 | 待核验 | 待核验 | 待核验 | 待核验 | Visible |

当前实现不是 Schema-faithful、Interaction-faithful 或 Integrated。

## 已创建文件

- `src/config/risk-pages.js`
  - 维护“悦禧风控”的独立页面配置。
  - 所有草案筛选、枚举、列均带 `verified: false`。
  - `originalUrl` 和 `navid` 保持空值，不虚构原地址。
- `src/views/erp/risk-workbench/index.vue`
  - 风控专属工作台。
  - 页面顶部固定显示“待原系统二次核验”和 Visible 完成等级。
  - 三个页内标签都是领域承载草案，不冒充原 ERP 结构。
  - 只提供本地筛选和查看草案详情，不提供保存、审核、处置、删除、导出等可能被误认为原系统工具栏的动作。
- `src/api/erp-risk.js`
  - 只包含只读列表请求。
- `mock/erp-risk.js`
  - 只返回空列表和证据状态；页面演示行全部为脱敏本地草案。
  - 明确 `persisted: false`。

## 根任务集成清单

为避免并行修改冲突，本任务未修改共享文件。根任务需要完成：

1. `src/config/erp-menu.js`

在 `specialPageTypes` 判断之前增加：

```js
if (groupKey === 'risk') return 'risk-workbench'
```

这会取代当前 `悦禧风控 -> risk` 的通用页面映射。

2. `src/router/index.js`

增加组件懒加载和页面类型映射：

```js
const riskWorkbenchPage = () => import('@/views/erp/risk-workbench/index')
```

```js
'risk-workbench': riskWorkbenchPage
```

3. `mock/index.js`

```js
const erpRisk = require('./erp-risk')
```

并将 `...erpRisk` 加入 mocks。

## 原系统二次审计清单

根任务取得已登录浏览器后，应按一页一矩阵完成：

1. 从原 ERP 一级菜单读取“悦禧风控”的真实 `href`、`navid`、打开方式和权限可见性。
2. 判断页面是本地页面、外部系统跳转、iframe、仪表盘、列表、树、表单还是混合页面。
3. 记录首次进入时的默认标签、默认门店、默认日期范围、默认查询条件和初始排序。
4. 记录查询区每个控件的标签、类型、占位符、完整选项顺序、默认值、禁用状态和联动。
5. 记录顶部工具栏准确标签、顺序、位置及行选择要求；只读打开可逆弹窗，不点击保存、提交、处置、删除、导出或其他写入动作。
6. 记录每个表格的可见列、隐藏技术列、状态/金额/日期格式、汇总行和分页。
7. 只读打开详情、新增、编辑、规则、处置等可逆弹窗，记录字段、必填标记、提示、附件、审批和通知字段。
8. 如存在规则条件构造器，逐项记录字段、运算符、阈值、逻辑组合、启停状态和依赖清空规则。
9. 用实证替换 `risk-pages.js` 中相应草案；每个字段验证后才能改为 `verified: true`。
10. 全量回归本地路由、刷新、每个下拉分支、弹窗、控制台错误，并确认不存在通用工作台自动注入的按钮。

## 集成仍待完成

- 原 ERP 真实页面契约审计。
- 风险规则引擎、风险事件生成、处置状态机和不可篡改操作日志。
- 与客户、合同、收付款、入住、护理、产康、膳食和仓存的真实事件集成。
- 风险数据访问权限、敏感字段脱敏、附件安全、消息通知与审批。
- 生产后端 API、持久化、幂等、审计与权限校验。
