# 并行任务：数据库、权限与部署

> 任务边界：仅处理 `server/`、`scripts/`、`database/`、`docs/database/` 和部署/环境配置；不修改小程序、`src/views/` 或 `mock/erp-demo.js`。
> 工作树说明：本任务开始时主任务已经修改多个前端业务文件和总进度文档，本任务不覆盖或提交这些改动。

## 2026-07-31

### 现状审计

- 数据库基线由 `database/mysql/reference/yuezi-legacy-schema-20260725.sql`、`V001`～`V003` 组织员工迁移和 `V004`～`V019` 业务迁移共同组成。
- `server/mvp_api.py` 的运行时迁移清单手工硬编码为 `V004`～`V019`，新增迁移容易漏注册，且没有数据库级并发迁移锁。
- `schema_migrations` 已保存 SHA-256，可发现已执行迁移被篡改；但缺少“迁移目录连续性、未知已执行版本、待执行版本”的离线/上线前检查。
- `scripts/yuezi_mysql_setup.py` 的备份能力只导出表结构；仓库没有全量业务数据备份、校验清单和受保护的恢复流程。
- API 默认连接用户为 `root`；开发模式允许固定本地令牌密钥回退；生产模式没有统一的必填配置校验。
- API 已对大部分业务查询使用 `tenant_id + store_id` 条件，普通账号无门店时会 fail-closed。
- `_allowed_store` 对 `SYS_ADMIN` 存在绕过：管理员可提交不在 `user_stores` 中的任意 `store_id`。由于数据库外键只约束 `store_id` 本身，理论上可能写入“当前租户记录 + 其他租户门店”的不一致关联。
- 登录态读取角色和门店时未显式校验角色/门店与账号属于同一租户，依赖导入数据正确性，不适合作为生产防线。
- `role_data_scopes` 和字段权限结构已经存在；当前业务接口主要落实到门店级隔离，部门/本人范围尚未全面进入各业务表查询条件。
- `.env.mvp` 能让前端改走正式 API，`.env.development` 保留 Mock；但缺少上线环境样例、切换/回退步骤和反向代理边界说明。

### 分阶段计划

1. **P0 上线安全基线**
   - 迁移改为按文件发现，增加版本/校验和检查与数据库级迁移锁。
   - 生产环境启动时校验令牌密钥、数据库账号、CORS、监听地址等关键配置。
   - 健康检查拆分为存活与就绪检查，限制请求体大小并补安全响应头。
2. **P0 权限与数据隔离**
   - 管理员写操作也必须服从显式 `user_stores` 白名单。
   - 账号读取角色、门店、权限时校验同租户和有效状态。
   - 增加数据库完整性审计及门店隔离离线测试；记录部门/本人范围后续接表条件。
3. **P0 数据保护与运维**
   - 增加使用 `mysqldump`/`mysql` 客户端的全量备份、清单校验、受保护恢复工具。
   - 补环境变量样例、部署手册、Mock/正式 API 切换和上线检查清单。
   - 在无真实数据库凭据时执行 Python/Node 静态检查、单元测试和前端构建；真实迁移、备份恢复演练列为运维准入条件。

### 已完成

- 2026-07-31：完成指定文档、`server/`、`scripts/`、`database/mysql/`、`docs/database/` 和现有环境配置审计。
  - 检查入口：`git status --short`、PowerShell 文件检索、源代码和 SQL 逐段审查。
  - 结果：确认范围内文件起始时无他人修改；主任务前端改动保持不动。
  - 仍需条件：无。

### 测试记录

- 2026-07-31：`python -m py_compile server/runtime_security.py server/erp_read_surfaces.py server/mvp_api.py scripts/mysql-backup.py scripts/audit-database-security.py scripts/test-runtime-security.py scripts/test-mysql-backup.py`
  - 结果：通过；新增 Python 文件及修改后的 API 文件均可编译。
- 2026-07-31：`npm run test:runtime:security`
  - 结果：9/9 通过；覆盖迁移连续性、校验和/未知版本、生产配置、远程数据库 TLS、管理员显式门店白名单和无门店 fail-closed。
- 2026-07-31：`npm run test:mysql:backup`
  - 结果：3/3 通过；覆盖备份清单/SHA-256、篡改拒绝和演练库目标保护。
- 2026-07-31：`npm run build:api`
  - 结果：通过；生成 `/api` 正式接口构建。Webpack 仅报告既有包体积告警，主入口约 3.57 MiB。
- 2026-07-31：`pnpm install --offline --frozen-lockfile --config.confirmModulesPurge=false`
  - 结果：通过；`pnpm-workspace.yaml` 固化原锁文件 overrides，并显式拒绝 7 个旧依赖包的安装脚本，避免非交互环境出现未审批脚本执行。
- 2026-07-31：`python scripts/mysql-backup.py check-tools`
  - 结果：当前环境未安装 `mysql`、`mysqldump`，因此未执行真实全量备份/恢复；上线主机必须安装兼容客户端后复测。
- 2026-07-31：`git diff --check`
  - 结果：通过；仅有 Git 的 LF/CRLF 提示，无空白错误。
- 未执行：`npm run migrate:mvp`、`npm run verify:mvp`、`npm run audit:db:security`、`npm run test:mvp:rbac`。
  - 原因：当前任务环境没有 `ERP_DB_PASSWORD`、代表账号密码和可连接 MySQL；不能伪造真实数据库测试结果。

### 本次完成项

- 2026-07-31：运行时迁移从手工清单改为 `V*.sql` 自动发现，检查连续序号、SHA-256、未知数据库版本并使用 MySQL advisory lock 防并发。
- 2026-07-31：新增首次结构基线迁移入口 `npm run migrate:mysql:baseline`；日常迁移继续使用 `npm run migrate:mvp`。
- 2026-07-31：生产环境强制最小权限数据库账号、32 字节令牌密钥、显式 HTTPS CORS；远程数据库强制 CA，配置不安全时拒绝启动。
- 2026-07-31：增加 `/api/health`、`/api/ready`、请求体上限、安全响应头、登录失败锁定和生产环境禁用 URL query token。
- 2026-07-31：`SYS_ADMIN` 改为和其他账号一样服从显式 `user_stores`；登录角色、权限、字段权限和门店按同租户/有效状态读取。
- 2026-07-31：补客户—业务员、客户—合同、合同—收款、合同—订房和房间冲突的租户/门店一致性检查。
- 2026-07-31：组织、员工、月嫂等通用列表手机号默认脱敏；客户手机号继续服从字段权限。
- 2026-07-31：新增只读数据库完整性审计、离线权限测试、全量备份/清单校验/受保护恢复工具。
- 2026-07-31：新增 MySQL 单机试运行样例、Nginx HTTPS/API 代理样例、systemd 服务样例、生产环境变量模板和上线清单。
- 2026-07-31：新增 `.env.api`/`build:api`，正式 API 构建关闭开发 Mock；`npm run dev` 保留本地 Mock 回退模式。

### 待主任务合并/关注

- 本提交不包含主任务已修改的前端业务页、`mock/erp-demo.js`、总进度文档或日报图片。
- `vue.config.js` 仅增加“正式 API 模式不注册 Mock server”的部署开关；`.env.api` 使用 `/api`，需配套 Nginx 去前缀代理。
- 前端依赖安装提示 Vue 2 EOL、Axios 0.18.1 严重漏洞及多项旧依赖停止维护；需另开升级任务，不能把当前构建通过等同于供应链安全通过。
- 当前 API 仍是 `ThreadingHTTPServer`，只能按受控内网试运行口径交付；公网生产化仍需服务框架、令牌撤销、集中日志、指标和统一限流。

### 待甲方/运维提供

- 生产 MySQL 地址、端口、数据库名、最小权限应用账号和迁移专用账号。
- 备份存储位置、保留天数、加密/密钥托管方案及恢复目标（RPO/RTO）。
- 正式 ERP 域名、HTTPS 证书、反向代理/WAF、允许的 CORS 来源和内网访问策略。
- 两店管理员、财务、销售、护理、产康、客房等代表账号及最终跨店/跨部门例外名单。
- 部门/本人数据范围所需的各业务表责任人字段映射与甲方确认口径。

## 2026-07-31 P0 真实数据库主链与双店 RBAC 验收

### 环境审计

- 未发现 MySQL/MariaDB 服务；`127.0.0.1:3306`、API `3000`、前端 `9527/9530` 均未监听。
- 当前解释器无 PyMySQL，环境中无数据库、Token 或角色测试密码。
- 未安装系统软件，未连接生产库，未创建或修改数据库记录。

### 完成项

- 新增 `scripts/p0_local_acceptance.py`：
  - 仅允许回环数据库/API并拒绝生产、预发布运行环境；
  - 写操作要求显式 `LOCAL_TEST_ONLY` 确认；
  - 创建 9 个 `TEST_P0_` 双店代表账号和两店哨兵客户；
  - 校验管理员、店长、销售、财务、护理角色、按钮和跨店 403；
  - 中心店、黄河路分别执行客户→合同→收款→订房→入住；
  - 直接核验 MySQL 最终状态，并提供自动/中断清理。
- 新增 `scripts/test_p0_local_acceptance_offline.py`。
- 新增 `docs/验收/P0-真实数据库主链与双店RBAC验收-2026-07-31.md`。

### 测试

| 命令/入口 | 结果 |
| --- | --- |
| `python -m py_compile scripts/p0_local_acceptance.py scripts/test_p0_local_acceptance_offline.py` | 通过 |
| `python scripts/test_p0_local_acceptance_offline.py` | 9/9 通过 |
| `python scripts/test-runtime-security.py` | 9/9 通过 |
| `python scripts/test-mysql-backup.py` | 3/3 通过 |
| `python -m compileall -q server scripts` | 通过 |
| `npm run build:api` | 通过；仅有既存的 3 项包体积警告 |
| `python scripts/p0_local_acceptance.py preflight` | 按设计阻塞，退出码 2；报告无凭据、3306/API 未监听、PyMySQL 缺失 |

### 阻塞和待提供条件

- 可丢弃或可恢复的本地/预发布 MySQL 5.7 测试库。
- 迁移账号、应用账号、数据库密码、本地 Token 密钥和临时 TEST 密码。
- 项目 Python 虚拟环境及 PyMySQL。
- 正式角色编码、按钮边界和跨店例外确认。
- 两店各一间可用于未来日期验收的空闲房。

真实迁移、bootstrap、API 主链和 RBAC 结果均为“未执行”，不得据此声明验收通过或已经上线。

## 2026-07-31 本机环境补齐与真实 P0 复验

> 本节是同日后续执行结果，取代上方“本机环境阻塞”的阶段性结论。所有写入仅发生在
> `127.0.0.1` 的全新本地测试库，未连接生产库，也不代表已经生产上线。

### 本机环境与密钥

- 复核 `QideYueziMySQL` 为 Automatic/Running，MySQL Community Server
  `8.4.9` 仅监听 `127.0.0.1:3306`，`mysqlx=0`。
- root 初始空密码已立即替换；空密码登录已被拒绝，带保护凭据的 root 登录通过。
- root、迁移、应用、TEST、Token 和 bootstrap 密钥均为强随机值，仅以当前 Windows
  用户 DPAPI 加密形式保存于
  `D:\QideYueziERP\secrets\erp-local-secrets.clixml`。
- 密钥文件关闭继承，仅当前 Windows 用户与 `SYSTEM` 可访问；Git 中没有明文密钥，
  临时 MySQL option 文件均已删除。
- 创建 `yuezi` 数据库、`yuezi_migrator@127.0.0.1` 和
  `yuezi_app@127.0.0.1`。迁移账号仅拥有 `yuezi.*`，应用账号仅拥有
  `SELECT/INSERT/UPDATE/DELETE/EXECUTE`。
- 隔离 Python 环境为 `D:\QideYueziERP\venv`，安装 PyMySQL 和
  `cryptography`，未污染系统 Python。

### 实施修复

- 新增 `scripts/yuezi_mysql_setup.py bootstrap-minimal`：
  - 只允许目标库名精确为 `yuezi`；
  - 只接受已导入 reference schema、无 tenant、无迁移记录的全新库；
  - 先写 tenant/store，再执行 V001～V003、种子 RBAC，最后执行
    V004～V019，解决 V010 在 tenant/store 之前执行时的外键失败。
- 新增规范角色 `STORE_MANAGER`，两店店长使用同一角色编码并由
  `user_stores` 决定门店范围。
- 收紧 `SALES_CONSULTANT`：移除 `FINANCE.CREATE` 和 `ROOM.CREATE`，
  保持销售、财务、店长职责分离。
- `server/mvp_api.py bootstrap` 改为逐门店判断房间库存；黄河路已有房间时，
  仍会为缺房的中心店补齐测试房间。
- P0 最终状态查询在读取房间快照后显式结束 `REPEATABLE READ` 只读事务，
  可看到后续 API 连接已提交的订房和入住关系。

### 真实迁移、API 与权限结果

| 日期 | 命令/入口 | 结果 |
| --- | --- | --- |
| 2026-07-31 | reference schema SHA-256 校验并导入 | 哈希一致；102 张基线表；临时 ASCII 路径副本已删除 |
| 2026-07-31 | `scripts/yuezi_mysql_setup.py bootstrap-minimal --database yuezi` | 从空库执行 V001～V019 共 19 项；1 tenant、2 stores、20 roles、141 permissions |
| 2026-07-31 | `server/mvp_api.py migrate` | V004～V019 全部 `already-applied` |
| 2026-07-31 | `server/mvp_api.py bootstrap` | 本地 admin 创建/更新成功；逐店补齐中心店 2 间房；两店合计 26 间 |
| 2026-07-31 | `server/mvp_api.py verify` | schema current；无 pending、checksum mismatch 或 unknown migration；安全完整性通过 |
| 2026-07-31 | `scripts/audit-database-security.py` | 8 类跨租户/跨店完整性检查均为 0，`status=passed` |
| 2026-07-31 | `GET http://127.0.0.1:3000/api/ready` | HTTP 200，迁移 current；数据源日志明确为 MySQL，无 JSON/mock fallback |
| 2026-07-31 | `scripts/p0_local_acceptance.py preflight` | ready；双店、5 类角色、19 项迁移均可用 |
| 2026-07-31 | `scripts/p0_local_acceptance.py all` | 通过；9 个代表账号按钮权限和门店范围通过；两店主链均到“已入住”；跨店合同审核、收款审核、入住共 6 次返回 403 |
| 2026-07-31 | `scripts/p0_local_acceptance.py cleanup` + SQL 计数 | 通过；TEST 账号、客户、合同、收款计数均为 0 |

第一次 `all` 因验收脚本持有旧事务快照报告“incomplete MySQL relationship”，其
`finally` 和外层 `cleanup` 均成功；修正事务边界后第二次完整通过。该失败和修复过程
保留在验收记录中，未将首次失败伪造成成功。

### 备份与离线回归

| 日期 | 命令/入口 | 结果 |
| --- | --- | --- |
| 2026-07-31 | `scripts/mysql-backup.py check-tools` | `mysql`、`mysqldump` 均为 8.4.9，可用 |
| 2026-07-31 | `scripts/mysql-backup.py backup --database yuezi ...` | 成功生成 `D:\QideYueziERP\backups\yuezi-local-p0-20260731.sql.gz` 和 manifest；压缩后 35,509 bytes |
| 2026-07-31 | `scripts/mysql-backup.py verify --backup ...` | gzip/UTF-8 与 SHA-256 独立复验通过 |
| 2026-07-31 | `python scripts/test_p0_local_acceptance_offline.py` | 9/9 通过 |
| 2026-07-31 | `python scripts/test-runtime-security.py` | 9/9 通过 |
| 2026-07-31 | `python scripts/test-mysql-backup.py` | 3/3 通过 |
| 2026-07-31 | `python -m py_compile ...` | 本次修改的服务端、初始化、验收、备份和审计脚本均通过 |

备份文件为本机验收快照，不含本次已清理的 `TEST_P0_*` 测试数据；尚未执行恢复到
scratch 库，生产恢复演练仍需运维环境、RPO/RTO 和恢复窗口。

### MySQL 8.4 相对 5.7 基线的结论与风险

- 已验证：5.7 标注的 reference schema 能导入 MySQL 8.4.9；19 项迁移能完整执行；
  bootstrap、应用 DML、双店 RBAC、两条交易主链和 `mysqldump` 均通过。
- 本次暴露的是初始化数据顺序和事务快照问题，不是已确认的 8.4 SQL 语法不兼容。
- 仍有风险：尚未导入甲方完整生产数据和私有员工名册；未覆盖生产数据量、慢查询、
  字符集脏数据、5.7→8.4 原地升级路径、复制/高可用、远程 TLS 与真实恢复耗时。
- 项目兼容基线仍写 MySQL 5.7；在甲方确认升级前，不应仅凭本机 8.4 验收改变生产
  版本目标。

### 仍需甲方/运维提供

- 正式 MySQL 版本、地址、TLS CA、迁移窗口、生产最小权限账号和审计策略。
- 完整脱敏生产快照与私有员工名册，用于 5.7/8.4 差异和数据质量复验。
- 生产域名、HTTPS/CORS、反向代理、WAF、日志、监控、告警和备份异地存储。
- RPO/RTO、保留周期、加密密钥托管方案，以及 scratch/预发布恢复演练窗口。
- 店长、销售、财务、护理最终按钮边界与跨店例外的业务签字确认。
