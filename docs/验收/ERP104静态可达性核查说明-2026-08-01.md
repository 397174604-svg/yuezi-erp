# ERP 104 项静态可达性核查（2026-08-01）

## 本次口径

- 检查源：`src/config/erp-feature-registry.js`、`src/router/index.js`、`server/mvp_api.py`。
- 不调用接口、不改数据库、不改房态或智能排房。
- 104 项注册、菜单分配和正式路由是三个不同层级；菜单可见不等同于接口已实现。

## 运行方式

```powershell
python scripts/audit_erp104_static.py
```

机器可读结果：`docs/验收/ERP104静态可达性核查.json`。脚本发现 `FAIL` 会以退出码 `1` 结束，这是发布门禁，不代表脚本自身执行异常。

## 本次结果

- 注册表：104 项，104 个唯一功能编号。
- 菜单/正式路由分配：104 项；`F001`、`F002` 合理合并为唯一的“总部驾驶舱”入口。
- 未通过：16 项；风险提示：7 项。

### 未通过项（不得标记完成）

1. `F027`、`F069`、`F111`、`F112`、`F115`、`F120`、`F121`、`F122`：页面已停止请求不存在的 `/vue-element-admin/erp/baby/modules/*`，不会再裸露 404 或演示数据；但后端资源仍待实现，因此不能标记为完整业务闭环。
2. `F099`–`F106`：产康功能页面所需资源名未在 recovery handler 白名单中，存在确定的 404/403 风险。

### 需人工验收的通用壳风险

- `F010`、`F108` 已切换为独立审批工作台，不再回退收款页面；审批聚合接口仍按页面提示待接入。
- `F017`、`F086` 已切换为独立预约工作台，使用现有产康预约/人员排班资源，不再复用客房房态页面。
- 组织与绩效 11 项已切换为独立工作台，不再复用系统设置的示例 A–H 页面。
- 护理、销售、财务、仓存等多个功能共用工作台组件。复用本身不判失败，但每个功能必须用不同资源、字段、状态流转和写入测试证明不是重复页面。

## 后续要求

- 修复后重新运行本脚本；只有 `failCount=0` 才能通过静态发布门禁。
- 本检查只验证静态可达性；仍需执行登录、角色、读写、导出、门店隔离和浏览器冒烟验收。
