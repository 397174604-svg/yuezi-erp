# ERP 104 非客户运营模块：重复与接口复核

复核时间：2026-08-01
复核方式：先静态只读定位重复外壳，再对路由与专属工作台进行修复后复核；未触碰智能排房或客房房态。

## 保护范围

- F018 客房房态、F019 智能排房均为冻结项。本次只写明“受保护”，未进入页面、接口、数据或规则检查。

## 重复或路由错配修复结果

| 功能编号 | 结论 | 证据 | 修复责任域 |
| --- | --- | --- | --- |
| F010 审批中台 | 已修复：改为独立 `approval-workbench`，不再回退到收款管理；未配置外部审批资源时明确显示待接入，不伪造审批成功。 | `src/views/erp/approval-workbench/index.vue`、`src/config/approval-pages.js` | 财务与审批 |
| F017 预约与排班 | 已修复：改为独立 `schedule-workbench`，使用预约/技师排班领域资源，不再导向客房通用页。 | `src/views/erp/schedule-workbench/index.vue`、`src/config/schedule-pages.js` | 预约与排班 |
| F086 在线预约看板 | 已修复：与 F017 共用预约领域服务，但通过独立功能配置展示在线预约看板语义，不再导向客房壳。 | `src/views/erp/schedule-workbench/index.vue`、`src/config/schedule-pages.js` | 预约与排班 |

## 接口或持久化缺口

| 功能编号 | 风险 | 必须补齐的闭环 | 责任域 |
| --- | --- | --- | --- |
| F012 收支分析、F015 成本核算、F016 充值报表、F080 厂商并行期对账帮手 | 财务页不存在专属配置，均可能回退成收款管理。 | 专属资源、查询口径、导出与权限；未完成前显示“待接入”，不可冒充收款页。 | 财务与审批 |
| F083 在线支付 | 目前只能确认收款/对账接口，未发现渠道适配闭环。 | 支付申请、待执行、失败原因、重试、对账记录；不显示第三方已支付。 | 财务与审批 |
| F107 电子合同 | 销售页会回退合同管理，未发现电子签署资源。 | 合同模板、签署申请、待接入/失败状态、回调审计；不伪造签约完成。 | 销售合同 |
| F058 门店与渠道、F093 连锁多门店管理 | `store-workbench` 使用内存 `createRows/saveRecord`。刷新后记录丢失。 | 持久化 API、来源/目标门店、审核人、生效时间、审计记录。 | 门店资产 |
| F061 系统设置、F079 历史数据迁移、F098 品牌定制 | `system-workbench` 使用内存行数据；系统模块路由存在但该页未接入读写。 | 对应资源、保存、发布/回滚/预检、审计与失败记录。 | 平台与系统设置 |

## 合理的共享壳（不应作为重复删除）

- F007 收银开单、F009 财务收支、F011 退款与报销、F013 发票、F014 交易对账共用 `finance-workbench`，但各自具有独立资源键和不同业务字段；共享外壳合理。
- F020 合同与销售、F050 套餐管理共用 `sales-workbench`，存在独立 `contracts`、`packages` 资源；共享外壳合理。

## 上线前的最小修复次序

1. F010、F017、F086 的专属入口已经完成；继续保留 F018/F019 冻结版本不被改动。
2. 为 F012/F015/F016 建立专属配置与资源，禁止未知标题回退成收款管理。
3. 把 F058/F061/F079/F093/F098 从内存示例切换到可保存、可查询、可审计的 API；未完成时明确标记“开发中/待接入”。
4. F083、F107 采用内部业务闭环；未配置第三方时只显示待执行或失败原因。
5. 对上述修复执行管理员、销售、财务三角色的进入、查询、保存、状态流转、导出和浏览器控制台冒烟。

## 本次边界

- 本报告对应的修复只涉及业务路由、专属页面及验收脚本；未写入数据库或房态数据。
- 未删除、调整或读取 F018/F019 的页面与接口实现。
- 该报告只判断静态可见风险；“可用”仍需在合并后以接口与浏览器冒烟结果为准。
